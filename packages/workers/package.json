{
  "name": "journal-web-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.8.0",
    "drizzle-orm": "^0.30.10",
    "ioredis": "^5.4.1",
    "node-cron": "^3.0.3",
    "journal-web-api": "workspace:*",
    "journal-web-crawler": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5"
  }
}
